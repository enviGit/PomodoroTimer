<Window x:Class="PomodoroTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        xmlns:local="clr-namespace:PomodoroTimer"
        mc:Ignorable="d"
        Title="Pomodoro Timer" Height="450" Width="350" Icon="Resources/appIcon.png" ResizeMode="CanMinimize">
    <Grid>
        <Image gif:ImageBehavior.AnimatedSource="Resources/bg.gif"/>

        <TextBlock x:Name="TimeRemainingLabel" Text="" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="48" Margin="0,0,0,125"/>

        <ProgressBar x:Name="ProgressBar" Value="0" Minimum="0" Maximum="1" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="10" Visibility="Collapsed"/>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <Button x:Name="StartButton" Margin="10" Width="50" Height="50" BorderBrush="{x:Null}" Click="StartButton_Click">
                <Button.Background>
                    <ImageBrush ImageSource="Resources/startButton.png" Stretch="Fill"/>
                </Button.Background>
            </Button>
            <Button x:Name="PauseButton" Margin="10" Width="50" Height="50" BorderBrush="{x:Null}" Click="PauseButton_Click">
                <Button.Background>
                    <ImageBrush ImageSource="Resources/pauseButton.png" Stretch="Fill"/>
                </Button.Background>
            </Button>
            <Button x:Name="ResetButton" Margin="10" Width="50" Height="50" BorderBrush="{x:Null}" Click="ResetButton_Click">
                <Button.Background>
                    <ImageBrush ImageSource="Resources/resetButton.png" Stretch="Fill"/>
                </Button.Background>
            </Button>
        </StackPanel>

        <ListBox x:Name="SessionList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10" Background="{x:Null}" BorderBrush="{x:Null}" Visibility="Collapsed">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding}" Margin="5"/>
                        <!--<Image Source="check.png" Width="16" Height="16" Visibility="{Binding IsCompleted, Converter={StaticResource BooleanToVisibilityConverter}}"/>-->
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>